/**
 * Primitive types and shared type definitions
 *
 * @module parser/rule-based/types/primitives
 */

/** Unique identifier for a rule configuration */
export type RuleId = string;

/** Allowed types for variables extracted from tags */
export type TagVarValue = string | number | boolean;

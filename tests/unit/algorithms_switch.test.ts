import { algorithmSwitchData } from "src/algorithms/algorithms_switch";

describe("algorithmSwitchData", () => {
    test("to fsrs", () => {
        // algorithmSwitchData(new SRPlugin(app, null), algorithmNames.Default, algorithmNames.Fsrs);
    });
});
